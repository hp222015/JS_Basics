const square = n =>{
      return n*n;
}
console.log(square(2));

const printFullName = (firstName, lastName) => {
      return `${firstName} ${lastName}`
    }
console.log(printFullName("Harshit","Pawar"));